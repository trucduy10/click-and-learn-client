import { createSelector } from "@reduxjs/toolkit";

const partReducer = (state) => state.part;

// export const selectPartsByCourseId = createSelector(
//   [partReducer],
//   (partReducer) => partReducer
// );
